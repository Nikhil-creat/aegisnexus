# Copy to .env  (never commit .env)
ANTHROPIC_API_KEY=
LLM_MODEL=claude-sonnet-5
ADMIN_USER=admin
# Choose a strong password (10+ chars). Leave empty to auto-generate one (see `docker compose logs api`).
ADMIN_PASSWORD=
AUTH_REQUIRED=true
RATE_LIMIT_PER_MIN=240
